/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { FormationService } from "../formation.service";
import { FormationCreateInput } from "./FormationCreateInput";
import { Formation } from "./Formation";
import { FormationFindManyArgs } from "./FormationFindManyArgs";
import { FormationWhereUniqueInput } from "./FormationWhereUniqueInput";
import { FormationUpdateInput } from "./FormationUpdateInput";

export class FormationControllerBase {
  constructor(protected readonly service: FormationService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Formation })
  async createFormation(
    @common.Body() data: FormationCreateInput
  ): Promise<Formation> {
    return await this.service.createFormation({
      data: data,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Formation] })
  @ApiNestedQuery(FormationFindManyArgs)
  async formations(@common.Req() request: Request): Promise<Formation[]> {
    const args = plainToClass(FormationFindManyArgs, request.query);
    return this.service.formations({
      ...args,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Formation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async formation(
    @common.Param() params: FormationWhereUniqueInput
  ): Promise<Formation | null> {
    const result = await this.service.formation({
      where: params,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Formation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateFormation(
    @common.Param() params: FormationWhereUniqueInput,
    @common.Body() data: FormationUpdateInput
  ): Promise<Formation | null> {
    try {
      return await this.service.updateFormation({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Formation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteFormation(
    @common.Param() params: FormationWhereUniqueInput
  ): Promise<Formation | null> {
    try {
      return await this.service.deleteFormation({
        where: params,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
